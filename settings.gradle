dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "MorseDeCoder"
include ':app'
include ':multimon'
project(':multimon').projectDir = new File('multimon-android/multimon')